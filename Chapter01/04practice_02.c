#include <stdio.h>

int main()
{
    int length, breadth;

    printf("What is the length of Rectangle\n");
    scanf("%d", &length);

    printf("What is the length of Rectangle\n");
    scanf("%d", &breadth);

    printf("The area of Rectangle is %d\n", length * breadth);
    return 0;
}